{
  "name": "riskpulse-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace @riskpulse/frontend",
    "dev:backend": "npm run dev --workspace @riskpulse/backend",
    "build": "npm run build --workspace @riskpulse/shared && npm run build --workspace @riskpulse/frontend && npm run build --workspace @riskpulse/backend",
    "test": "npm run test --workspace @riskpulse/frontend && npm run test --workspace @riskpulse/backend",
    "check": "npm run build && npm run test"
  }
}
